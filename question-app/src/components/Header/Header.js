import React from "react";
import { MainContainer, Heading } from "./Header.elements";

const Header = () => {
  return (
    <MainContainer>
      <Heading>Question App</Heading>
    </MainContainer>
  );
};

export default Header;
